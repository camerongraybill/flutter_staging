name: gh-pages 
permissions:
  contents: write

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:   
        working-directory: ./test_counter_app
    steps:
      - name: checkout files
        uses: actions/checkout@v3

      - name: build flutter files
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - run: | 
          flutter pub get
          flutter test
          flutter build web --base-href /test-counter/

      - name: deploy to gh pages
        uses: JamesIves/github-pages-deploy-action@v4
        with: 
          folder: build 
